:80 {
    root * /srv
    file_server
    # react-router uses client-side routing (BrowserRouter) — unknown paths
    # must fall back to index.html instead of 404ing.
    try_files {path} /index.html
}
