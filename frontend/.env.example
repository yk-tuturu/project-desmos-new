# Base URL of the backend API, no trailing slash.
# Baked in at build time (Vite), so this must be set before `npm run build` /
# the Docker image build — not something you can change at container runtime.
VITE_API_URL=http://localhost:3001
